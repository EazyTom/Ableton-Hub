"""Entry point for running Ableton Hub via `python -m src`.

Used by Briefcase to launch the application from a bundled installer.
Also supports running directly: python -m src
"""

from src.main import main

main()
